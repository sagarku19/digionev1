---
noteId: "56b1aaf0674311f1bffb2f446ab401a4"
tags: []

---

# Supabase folder cleanup — 2026-06-13

Removed stale, point-in-time snapshot files that were captured **2026-06-02 (pre-RLS rollout)** and superseded by the live DB + `migrations/` + the production-readiness audit. Several were not just stale but actively wrong (e.g. `rls-status.txt` reported RLS **off** on every table; live state is **on** for all 62). The rebuild source of truth is now `supabase/migrations/`.

## Deleted (14 files)

**Stale schema dumps (repo root `supabase/`)**
- `full-schema.sql` — Jun-2 `pg_dump` of `public`, pre-RLS, missing the 3 tables added 2026-06-13.
- `storage-schema.sql` — Jun-2 storage dump; still listed the legacy buckets dropped 2026-06-03.
- `schema-reference.sql` — Apr-13 schema, long obsolete.

**`supabase/exports/` (entire folder removed)**
- `rls-status.txt` — showed RLS off everywhere → false after rollout.
- `rls-policies.txt` — pre-rollout policy text; live now has 87 policies.
- `indexes.txt` — pre index-pass snapshot.
- `_seed-data-raw.sql` — raw seed dump (canonical seed is `supabase/seed.sql`).
- `storage-and-auth.sql` — Jun-3 storage/auth dump (see follow-up below).
- `INVENTORY.md` — stale live-DB audit (listed dropped buckets, "59 tables").
- `RLS-POLICIES.md` — pre-rollout RLS design doc; live differs.
- `RLS-TEST-CHECKLIST.md` — spent; the RLS rollout it gated is complete.

**`.claude/todo-later/` (superseded audits)**
- `2-2026-06-03-db-audit.md`, `3-2026-06-03-new-db-audit.md` — the work they describe (Stages 1–6) landed; superseded by `5-2026-06-13-db-production-audit.md`.
- `4-2026-06-04-discover-routes-removal.md` — the route removal it recommended is done.

## Kept (and why)
- **`supabase/migrations/*` (all 14)** — the rebuild history, registered in the live DB. Never delete casually.
- `supabase/export-everything.sql` — read-only query pack that regenerates what the deleted exports held; kept the tool, dropped the stale output.
- `supabase/seed.sql`, `supabase/SUPABASE.md`, `supabase/REBUILD.md` — active (SUPABASE.md updated; REBUILD.md banner-flagged for rewrite).
- `.claude/todo-later/1-…storage-followups.md` (open items), `5-…production-audit.md` (current), `0-TD.txt` (scratch notes).

## References repointed (so nothing dangles)
- `.claude/rules/security-model.md` — RLS section now points at the migration + live `pg_policies` + audit #5 (was: `RLS-POLICIES.md`, `INVENTORY.md`).
- `supabase/SUPABASE.md` — file-map, "At a glance" counts (62 tables / 12 functions / 4 buckets / 87 policies), and known-issues refreshed.
- `supabase/REBUILD.md` — correction banner added (its `pg_dump` + `storage-and-auth.sql` flow is superseded by the `migrations/` + Supabase CLI flow).
- `.claude/todo-later/5-…md` — back-links to the deleted #2/#3 audits removed.

## Follow-ups created by this cleanup
1. **Capture `storage.objects` RLS policies into a dedicated migration.** They were only documented in the deleted `storage-and-auth.sql`; the auth.users triggers are safe (they live in `00000000000000_baseline.sql`), but storage-object policies are thinly represented in `migrations/`. Re-export from the live DB into a new migration so a rebuild reproduces them.
2. **Rewrite `REBUILD.md`** around `supabase db reset` / `db push` instead of the manual `pg_dump` flow.
3. **Migration-file hygiene** (separate, approval-gated — see audit #5 §10 and the notes below). Not done in this pass.
